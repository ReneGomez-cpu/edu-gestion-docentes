import { icon } from './icons.js';
export function shell(user, page, content) {
  const nav = (id, label, symbol) => `<button class="nav-item ${page === id ? 'active' : ''}" data-route="${id}"><span>${icon(symbol)}</span>${label}</button>`;
  return `<div class="shell"><aside class="sidebar"><div class="brand"><span class="brand-mark">E</span><div><strong>EduGestión</strong><small>Portal institucional</small></div></div><nav>${nav('dashboard', 'Panel principal', 'dashboard')}${nav('teachers', 'Docentes', 'teachers')}${user.role === 'admin' ? nav('users', 'Usuarios', 'users') : ''}</nav><div class="sidebar-foot"><div class="user-badge"><span>${user.name.slice(0,1).toUpperCase()}</span><div><strong>${user.name}</strong><small>${user.role === 'admin' ? 'Administrador' : 'Usuario autorizado'}</small></div></div><button class="signout" data-action="signout">${icon('logout')} Cerrar sesión</button></div></aside><section class="main"><header><button class="mobile-menu" data-action="menu">☰</button><div><p class="eyebrow">GESTIÓN ACADÉMICA</p><h1>${page === 'dashboard' ? 'Resumen institucional' : page === 'teachers' ? 'Directorio docente' : 'Administración de usuarios'}</h1></div><div class="header-date">${new Intl.DateTimeFormat('es-SV',{dateStyle:'full'}).format(new Date())}</div></header>${content}</section></div>`;
}
